name: Prototype Guard (Disabled)

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Prototype guard disabled
        run: echo "Prototype guard checks removed; allowing all changes."
